extends layout

block content
	h1= name
	each route in routes
		div.route
			div.route-map
			h2= route.name
			each dir in route.directions
				div.direction
					h3= dir.name
					ol.times
						each time in dir.times
							li= time
