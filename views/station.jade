extends layout

block content
	h1= title
	each route in station.routes
		div.route
			h2= route.name
			each dir in route.directions
				div.direction
					h3= dir.name
					ol.times
						each time in dir.times
							li= time
